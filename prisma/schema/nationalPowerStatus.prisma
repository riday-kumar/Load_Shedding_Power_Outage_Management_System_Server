model NationalPowerStatus {
    id             String   @id @default(uuid())
    date           DateTime @default(now())
    max_generation Decimal
    demand         Decimal
    shortage       Decimal
    createdBy      String

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}
